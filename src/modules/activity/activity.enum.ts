export enum ActivityStatus {
  HIDDEN = 'hidden',
  VISIBLE = 'visible',
}

export enum ActivityForWho {
  ADMIN = 'admin',
  USER = 'user',
}

export enum ActivityCategory {
  PROFILE = 'profile',
}
