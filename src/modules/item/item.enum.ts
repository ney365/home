export enum ItemStatus {
  SOLD = 'sold',
  BOUGHT = 'bought',
  CREATED = 'created',

  ON_SALE = 'on sales',
  OFF_SALE = 'off sales',
  PRIVATE = 'private',
  HIDDEN = 'hidden',
}

export enum ItemOwnership {
  CREATED = 'created',
  COLLECTED = 'collected',
}
